"use strict";
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spread = (this && this.__spread) || function () {
    for (var ar = [], i = 0; i < arguments.length; i++) ar = ar.concat(__read(arguments[i]));
    return ar;
};
Object.defineProperty(exports, "__esModule", { value: true });
var isPlainObject = function (value) {
    return value !== null && typeof value === 'object' && !Array.isArray(value);
};
var getAssetIds = function (node, acc) {
    var _a;
    if (acc === void 0) { acc = []; }
    if (Array.isArray(node)) {
        node.forEach(function (v) {
            getAssetIds(v, acc);
        });
    }
    if (isPlainObject(node)) {
        if (['file', 'image'].includes(node === null || node === void 0 ? void 0 : node._type) && ((_a = node === null || node === void 0 ? void 0 : node.asset) === null || _a === void 0 ? void 0 : _a._type) === 'reference') {
            acc.push(node.asset._ref);
        }
        Object.values(node).forEach(function (val) {
            getAssetIds(val, acc);
        });
    }
    return acc;
};
var getDocumentAssetIds = function (document) {
    var assetIds = getAssetIds(document);
    // Sort and dedupe
    return __spread(new Set(assetIds.sort()));
};
exports.default = getDocumentAssetIds;
//# sourceMappingURL=getDocumentAssetIds.js.map